#include <iostream>
#include <string.h>
#include <iomanip>
using namespace std;

int main(){
    int runs;
    cin >> runs;
    while(runs--){
        char x[1001],y[1001];
        int sum[1002]={0};
        gets(x);gets(y);
        int lenx = strlen(x), leny = strlen(y);
        
        
    }
}